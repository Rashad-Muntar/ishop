query GetStore($getStoreId: ID!) {
  getStore(id: $getStoreId) {
    id
    headerImg
    logo
    storeName
    address
    branches
    productCategories {
      items {
        id
        image
        title
      }
    }
  }
}


